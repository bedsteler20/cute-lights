{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "meson compile -C build && meson install -C build",
            "type": "shell",
            "args": [],
            "presentation": {
                "reveal": "always"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}