using Gtk 4.0;
using Adw 1;

template $CuteLightsWindow: Adw.PreferencesWindow {
  default-width: 300;
  default-height: 600;
  resizable: false;

  Adw.PreferencesPage {
    Adw.PreferencesGroup group {
      title: "Effects";
    }
  }
}
